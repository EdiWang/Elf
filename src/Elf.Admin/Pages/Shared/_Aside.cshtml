@{
    var currentPage = ViewContext.RouteData.Values["Page"]?.ToString();
}

<nav class="aside-nav">
    <ul>
        <li>
            <a class="elf-nav-link @(currentPage is "/Index" ? "active" : null)" asp-page="./Index">
                <span class="elf-icon" aria-hidden="true">☰</span>
                Links
            </a>
        </li>
        <li>
            <a class="elf-nav-link @(currentPage is "/Tags" ? "active" : null)" asp-page="./Tags">
                <span class="elf-icon" aria-hidden="true">#</span>
                Tags
            </a>
        </li>
        <li>
            <a class="elf-nav-link @(currentPage is "/Report" ? "active" : null)" asp-page="./Report">
                <span class="elf-icon" aria-hidden="true">◔</span>
                Report
            </a>
        </li>
    </ul>
</nav>