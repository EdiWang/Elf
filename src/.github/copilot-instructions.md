# Copilot Instructions

## Project Guidelines
- This repo follows a Fluent Web Components design principle. Prefer Fluent controls such as fluent-dropdown instead of replacing them with native HTML elements unless explicitly requested.